language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t ralfmason/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true ralfmason/docker-react npm run test
